'use strict';
let name = 'secureSpaceGenerator';
let price;
price = 1000;
const message = `Choose "${name}", a price for one thing is ${price} credits.`;
console.log(message);

price = 2000;
const secMessage = `Choose "${name}", a price for one thing is ${price} credits.`;
console.log(secMessage);

